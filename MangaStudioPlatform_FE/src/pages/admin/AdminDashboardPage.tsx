import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminDashboardPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Admin dashboard"
      description="the backend does not expose admin dashboard APIs yet."
    />
  );
}
