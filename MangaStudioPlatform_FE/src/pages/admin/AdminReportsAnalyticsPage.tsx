import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminReportsAnalyticsPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Reports and analytics"
      description="The backend does not expose admin reporting APIs yet."
    />
  );
}
