import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminWorkflowMonitoringPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Workflow monitoring"
      description="The backend does not expose admin workflow monitoring APIs yet."
    />
  );
}
