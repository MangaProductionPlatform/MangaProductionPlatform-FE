import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminModerationPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Moderation"
      description="The backend does not expose moderation APIs yet."
    />
  );
}
