import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminSeriesMonitoringPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Series monitoring"
      description="Use the creator Series page for current backend series records."
    />
  );
}
