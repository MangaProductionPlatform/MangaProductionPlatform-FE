import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminAiManagementPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="AI management"
      description="The backend does not expose AI management APIs yet."
    />
  );
}
