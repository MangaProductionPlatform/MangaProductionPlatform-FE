import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AdminNotificationsPage() {
  return (
    <EmptyBackendState
      eyebrow="Admin"
      title="Admin notifications"
      description="The backend does not expose notification APIs yet."
    />
  );
}
