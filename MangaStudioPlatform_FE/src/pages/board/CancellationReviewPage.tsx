import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function CancellationReviewPage() {
  return <EmptyBackendState eyebrow="Board" title="Cancellation review" description="The backend exposes cancel command but no review queue API yet." />;
}
