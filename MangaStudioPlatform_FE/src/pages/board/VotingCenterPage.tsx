import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function VotingCenterPage() {
  return <EmptyBackendState eyebrow="Board" title="Voting center" description="The backend does not expose board voting APIs yet." />;
}
