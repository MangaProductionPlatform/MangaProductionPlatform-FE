import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function PublishingSchedulePage() {
  return <EmptyBackendState eyebrow="Board" title="Publishing schedule" description="The backend has schedule commands but no queue/list API yet." />;
}
