import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function ReportsPage() {
  return <EmptyBackendState eyebrow="Board" title="Reports" description="The backend does not expose board report APIs yet." />;
}
