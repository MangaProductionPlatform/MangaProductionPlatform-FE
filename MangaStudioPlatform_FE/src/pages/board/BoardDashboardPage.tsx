import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function BoardDashboardPage() {
  return <EmptyBackendState eyebrow="Board" title="Board dashboard" description="Board dashboard APIs are not available yet." />;
}
