import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function BoardProfilePage() {
  return <EmptyBackendState eyebrow="Board" title="Board profile" description="Profile API is not available yet." />;
}
