import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function EditorWorkspacePage() {
  return <EmptyBackendState eyebrow="Editor" title="Editor dashboard" description="Editor workspace APIs are not available yet." />;
}
