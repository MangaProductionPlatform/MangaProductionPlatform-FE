import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function ReviewQueuePage() {
  return <EmptyBackendState eyebrow="Editor" title="Review queue" description="The backend does not expose an editor review queue API yet." />;
}
