import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function SeriesMonitoringPage() {
  return <EmptyBackendState eyebrow="Editor" title="Series monitoring" description="Use backend Series pages for real series data." />;
}
