import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AssistantSubmissionsPage() {
  return <EmptyBackendState eyebrow="Assistant" title="Submissions" description="The backend does not expose assistant submission history APIs yet." />;
}
