import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AssistantTasksPage() {
  return <EmptyBackendState eyebrow="Assistant" title="My tasks" description="The backend does not expose an assistant task list API yet." />;
}
