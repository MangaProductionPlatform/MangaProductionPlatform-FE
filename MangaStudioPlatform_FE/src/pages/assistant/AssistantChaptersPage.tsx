import { EmptyBackendState } from "../../shared/components/EmptyBackendState";

export default function AssistantChaptersPage() {
  return <EmptyBackendState eyebrow="Assistant" title="Assigned chapters" description="The backend does not expose assistant assigned-chapter APIs yet." />;
}
